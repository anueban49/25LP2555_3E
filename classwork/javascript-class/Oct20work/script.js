const addBtn = document.getElementById("addItem");
const list = document.getElementById("ListItems").value = [];
const newItem = document.getAnimations("textinput");
addBtn.addEventListener("click", () => {
    list.push(newItem);
})